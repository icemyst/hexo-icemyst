nav#nav
  span#blog-info
    a(href=url_for('/') title=config.title)
      if theme.nav.logo
        img.site-icon(src=url_for(theme.nav.logo))
      if theme.nav.display_title
        span.site-name=config.title

  #menus
    !=partial('includes/header/menu_item', {}, {cache: true})
    if (theme.algolia_search.enable || theme.local_search.enable || theme.docsearch.enable)
      #search-button
        a.site-page.social-icon.search(href="javascript:void(0);" title=' '+_p('search.title'))
          i.fas.fa-search.fa-fw

  .mask-name-container
    center(id="name-container")
      a(id="page-name" href="javascript:btf.scrollToDest()") PAGE_NAME

    #toggle-menu
      a.site-page(href="javascript:void(0);")
        i.fas.fa-bars.fa-fw

script.
  document.getElementById("page-name").innerText = document.title.split(" | ")[0];

  //- const pageNameElement = document.getElementById('page-name');

  //- window.addEventListener('scroll', () => {
  //-     if (window.scrollY > 50) {
  //-         pageNameElement.style.display = 'block';
  //-         pageNameElement.style.transition = 'all 0.3s';
  //-     } else {
  //-         pageNameElement.style.display = 'none';
  //-         pageNameElement.style.transition = 'all 0.3s';
  //-     }
  //- });